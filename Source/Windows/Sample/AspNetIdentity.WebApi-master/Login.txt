Login:
URI: http://localhost:59822/oauth/token
RequestBody: grant_type=password&username=anatoli&password=anatoli

Sample Code:
            var oauthClient = new OAuth2Client(new Uri("http://localhost:59822/oauth/token"), "client", "secret");
            try
            {
                var oauthresult = oauthClient.RequestResourceOwnerPasswordAsync("anatoli", "MySuperP@ss!", "foo bar").Result;
                if (oauthresult.AccessToken != null)
                {
                    Console.WriteLine(oauthresult.AccessToken);
                    Console.WriteLine();
                }
            }
            catch(Exception ex)
            {
                Console.WriteLine("Error, {0}", ex.Message);
            }
